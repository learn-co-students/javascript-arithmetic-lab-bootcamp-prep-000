
function add(a, b) {
  return a + b // returns the added value of a and b
}
function subtract(a, b) {
  return a - b // returns the value after b is subtracted from a
}
function multiply(a, b) {
  return a * b // returns the product of a and b
}
function divide(a, b) {
  return a / b // returns the value of a divided by b
}
function inc(n) {
  var number = 512
  n++ // 513
  return n ++ // increments n and returns the result
}
function dec(n) {
  var number = 512
  n-- // 511
  return n -- // decrements n and returns the result
}
function makeInt(n) {
  parseInt(n, 10) // n
  return parseInt(n, 10) // parses n as an integer and returns the parsed integer
}
function preserveDecimal(n) {
  parseFloat('n')
  return n // parses n as a floating point number and return the parsed number
}
