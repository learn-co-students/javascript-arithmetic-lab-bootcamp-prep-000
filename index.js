function add(a, b) {
  var number
  number = a + b
  return(number)
}

function subtract(a, b) {
  var number
  number = a - b
  return(number)
}

function multiply(a, b) {
  var number
  number = a * b
  return(number)
}

function divide(a, b) {
  var number
  number = a / b
  return(number)
}

function inc(n) {
  var number
  number = ++n
  return(number)
}

function dec(n) {
  var number
  number = --n
  return(number)
}

function makeInt(n) {
  var number
  number = parseInt(n,10)
  return(number)
}

function preserveDecimal(n) {
  var number
  number = parseFloat(n)
  return(number)
}
