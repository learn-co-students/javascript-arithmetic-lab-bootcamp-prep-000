var a = 3;
var b = 2;

function add(a, b) {
    return a + b;
}

add(a,b);

function multiply(a, b) {
    return a * b;
}

multiply (a,b);

function divide(a, b) {
    return a / b;
}

divide (a, b);

function subtract(a, b) {
    return a-b;
}

subtract(a,b);

var n;

function inc(n) {
    return n += 1;
}

inc(n);

function dec(n) {
    return n -= 1;
}

dec(n);

function makeInt(n) {
    return parseInt(n, 10);
}

makeInt(n);

function preserveDecimal(n) {
    return parseFloat(n);
}

preserveDecimal(n);
