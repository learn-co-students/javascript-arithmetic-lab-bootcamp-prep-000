

function add(a, b) {
  var number = a + b
  return number
}

function subtract(a, b) {
  var number = a - b
  return number
}

function multiply(a,b) {
  var number = a * b
  return number
}

function divide(a, b) {
  var number = a / b 
  return number
}

function inc(n) {
  n++
  return n
}

function dec(n) {
  n--
  return n
}

function makeInt(n) {
  n = parseInt(n, 10)
  return n
  } 
  
  
function preserveDecimal(n) {
  n = parseFloat(n)
  return n
}