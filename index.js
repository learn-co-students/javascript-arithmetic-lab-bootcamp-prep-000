let add = (x, y) => x + y
let subtract = (x, y) => x - y
let multiply = (x, y) => x * y
let divide = (x, y) => x / y
let inc = (x) => x + 1
let dec = (x) => x - 1
let makeInt = (x) => parseInt(x, 10)
let preserveDecimal = (x) => parseFloat(x)
