function add(a,b) {
  return parseInt(a)+parseInt(b)
}

function subtract(a,b) {
  return parseInt(a)-parseInt(b)
}

function multiply(a,b) {
  return parseInt(a)*parseInt(b)
}

function divide(a,b) {
  return parseInt(a)/parseInt(b)
}

n = a;

function inc(n) {
  return parseInt(n)+1
}

function dec(n) {
  return parseInt(n)-1
}

function makeInt(n) {
  var m = parseInt(n, 10);
    if ( m === 0 ) {
      return 0
      } else {
        return parseInt(n)
        }
}

function preserveDecimal(n) {
  return parseFloat(n)
}
